import styled from "styled-components";

export const TweetsView = styled.div`
  background: ${({theme}) => theme.cardBg1};
  border-radius: 10px;
  overflow: hidden;
  padding: 0;
  iframe{
      
  }
`
